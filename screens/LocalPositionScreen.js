import { View } from "react-native";

/* Required Functionality:
Display the user's current location on a map.
Display destinations on the map.
Allow the user to click on a destination.
Show a route from the user's current location to the clicked destination. */

const LocalPositionScreen = () => {
  return <View></View>;
};

export default LocalPositionScreen;
